import styled from 'styled-components';

const Wrapper = styled.section`
    h2 {
        margin-bottom: 50px;
    }

    #day-selector {
        margin-bottom: 30px;
    }
`;

export default Wrapper;
