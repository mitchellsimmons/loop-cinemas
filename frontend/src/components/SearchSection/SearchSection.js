import Wrapper from './SearchSection.styles';
import { Search } from 'components';

const SearchSection = () => {
    return (
        <Wrapper id='search-section'>
            <Search />
        </Wrapper>
    );
};

export default SearchSection;
